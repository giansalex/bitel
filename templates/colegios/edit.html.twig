{% extends 'base.html.twig' %}

{% block title %}Edit Colegios{% endblock %}

{% block body %}
    <h1>Edit Colegios</h1>

    {{ include('colegios/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('colegios_index') }}">back to list</a>

    {{ include('colegios/_delete_form.html.twig') }}
{% endblock %}
